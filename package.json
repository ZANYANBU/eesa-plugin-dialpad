{
  "name": "eesa-plugin-dialpad",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Eesa Dialpad plugin — per-tenant Dialpad phone system (calls, transcripts, AI recaps, call centers, contacts, directory, analytics, SMS) as MCP agent tools.",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/auth.js && node --check src/dialpad.js && node --check src/mcp.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
